# 🚀 Инструкция по деплою на GitHub

## Быстрый способ (через веб-интерфейс GitHub)

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name**: `text-editor-url` (или любое другое имя)
   - **Description**: "Текстовый редактор с синхронизацией через URL"
   - Выберите **Public** (для бесплатного GitHub Pages)
   - **НЕ** ставьте галочки на "Add README", "Add .gitignore" и "Choose a license"
4. Нажмите **"Create repository"**

5. Выполните команды в терминале (замените YOUR_USERNAME на ваш GitHub username):

```bash
cd "/Users/dmitriisharin/Desktop/курсор/Текст через ссылки"

# Добавить remote
git remote add origin https://github.com/YOUR_USERNAME/text-editor-url.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код
git push -u origin main
```

## Настройка GitHub Pages

1. Перейдите в репозиторий на GitHub
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Branch**: `main`
   - **Folder**: `/ (root)`
4. Нажмите **Save**
5. Через несколько минут ваше приложение будет доступно по адресу:
   `https://YOUR_USERNAME.github.io/text-editor-url/`

## Альтернативный способ (через GitHub CLI)

Если у вас установлен GitHub CLI:

```bash
cd "/Users/dmitriisharin/Desktop/курсор/Текст через ссылки"

# Создать репозиторий и отправить код
gh repo create text-editor-url --public --source=. --remote=origin --push

# Настроить GitHub Pages
gh api repos/YOUR_USERNAME/text-editor-url/pages -X POST \
  -f source[branch]=main \
  -f source[path]=/
```

---

После деплоя вы сможете открыть приложение по ссылке GitHub Pages и сразу протестировать!

